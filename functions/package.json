{
  "name": "the-arbitration-briefings-functions",
  "private": true,
  "main": "lib/functions/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "tsc --noEmit",
    "test": "npm run build && node mergeGate.test.mjs && node providers.test.mjs && node urlLiveness.test.mjs",
    "canary": "npm run build && node urlLiveness.canary.mjs"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "@google/genai": "^1.50.1",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.2.5",
    "openai": "^6.42.0"
  },
  "devDependencies": {
    "typescript": "~5.8.2"
  }
}
